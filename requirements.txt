psutil>=5.9.0         # for CPU and memory metrics
schedule>=1.2.0       # for event scheduling
prometheus-client>=0.18.0  # optional if using built-in collectors
requests>=2.31.0      # if any API calls are made (like alerts)
numpy>=1.24.0         # used in some memory/cpu scripts (if applicable)

